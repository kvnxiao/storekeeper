---
paths:
  - ".github/workflows/**"
---

# GitHub Actions Rules

Use for authoring and reviewing GitHub Actions workflows, especially action version pinning and update policy.

Invoke the `github-actions-rules` skill before writing or reviewing these files, then apply its reference rules.
